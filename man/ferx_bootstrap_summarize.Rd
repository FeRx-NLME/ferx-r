% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ferx_bootstrap.R
\name{ferx_bootstrap_summarize}
\alias{ferx_bootstrap_summarize}
\title{Re-summarise a finished bootstrap run}
\usage{
ferx_bootstrap_summarize(
  directory,
  skip_minimization_terminated = TRUE,
  skip_estimate_near_boundary = TRUE,
  skip_covariance_step_terminated = FALSE,
  skip_with_covstep_warnings = FALSE,
  ci = 95
)
}
\arguments{
\item{directory}{Path to a finished bootstrap run directory - one containing
\code{raw_results.csv}.}

\item{skip_minimization_terminated}{Exclude replicates whose minimization
terminated. Default \code{TRUE} (PsN's default).}

\item{skip_estimate_near_boundary}{Exclude replicates whose estimate sits on
a boundary. Default \code{TRUE} (PsN's default).}

\item{skip_covariance_step_terminated, skip_with_covstep_warnings}{Exclude
replicates on the covariance step's outcome. Both default \code{FALSE}, and
both require \code{keep_covariance = TRUE} - asking for one without it is an
error rather than a silently dropped filter.}

\item{ci}{Two-sided confidence level, in percent. Default 95.}
}
\value{
An object of class \code{ferx_bootstrap} with \verb{$parameters},
\verb{$diagnostics} and \verb{$raw} (read back from \code{raw_results.csv}), plus the run
metadata. \verb{$delta_ofv} is populated when the original run wrote a
\code{delta_ofv.csv}.
}
\description{
Recompute the statistics of a bootstrap run from its \code{raw_results.csv} under
a different set of exclusion criteria - PsN's \code{-summarize}. Refits nothing:
the estimates are already on disk, and so are the termination diagnostics
the filters read, so relaxing (say) \code{skip_minimization_terminated} is a
re-read rather than a re-run.
}
\details{
This is the recovery path for a run where too many replicates were filtered
out to resolve a percentile interval. It rewrites \code{bootstrap_results.csv}
and \code{bootstrap_diagnostics.csv} in \code{directory} in place; the per-replicate
files the original run wrote are still valid and are left alone.

Needs a run that was given a \code{directory}. \code{\link[=ferx_bootstrap]{ferx_bootstrap()}} with the default
\code{directory = NULL} computes in memory and writes nothing, so there is nothing
to re-summarise.
}
\examples{
\dontrun{
bs <- ferx_bootstrap(ferx_example("warfarin")$model, samples = 200,
                     directory = "warfarin-bootstrap")

# Keep the boundary cases after all, without refitting
relaxed <- ferx_bootstrap_summarize("warfarin-bootstrap",
                                    skip_estimate_near_boundary = FALSE)
relaxed$parameters
}

}
\seealso{
\code{\link[=ferx_bootstrap]{ferx_bootstrap()}}

Other fitting:
\code{\link[=ferx_bootstrap]{ferx_bootstrap()}},
\code{\link[=ferx_check_init]{ferx_check_init()}},
\code{\link[=ferx_collect]{ferx_collect()}},
\code{\link[=ferx_covariance]{ferx_covariance()}},
\code{\link[=ferx_fit]{ferx_fit()}},
\code{\link[=ferx_inits_from_nca]{ferx_inits_from_nca()}},
\code{\link[=ferx_sir]{ferx_sir()}},
\code{\link[=ferx_stop]{ferx_stop()}},
\code{\link[=print.ferx_job]{print.ferx_job()}}
}
\concept{fitting}
