% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ferx_sir.R
\name{ferx_sir}
\alias{ferx_sir}
\title{Run SIR against an existing fit}
\usage{
ferx_sir(
  fit,
  sir_samples = 1000L,
  sir_resamples = 250L,
  sir_seed = NULL,
  sir_keep_samples = FALSE,
  verbose = FALSE
)
}
\arguments{
\item{fit}{A \code{ferx_fit} object produced by \code{\link[=ferx_fit]{ferx_fit()}} or
\code{\link[=ferx_load_fit]{ferx_load_fit()}}.}

\item{sir_samples}{Number of proposal samples drawn from the asymptotic
distribution. Higher values give tighter weights at proportional cost.
Default 1000.}

\item{sir_resamples}{Number of resampled vectors. Must be \verb{<= sir_samples}.
Default 250.}

\item{sir_seed}{Integer RNG seed for reproducibility. \code{NULL} (the default)
uses the engine's built-in seed.}

\item{sir_keep_samples}{When \code{TRUE}, retain the resampled packed
parameter vectors on the returned fit. Required for
\code{\link[=ferx_simulate_with_uncertainty]{ferx_simulate_with_uncertainty()}} with \code{method = "sir"}. Default
\code{FALSE}.}

\item{verbose}{When \code{TRUE}, the engine prints progress to stderr.
Default \code{FALSE}.}
}
\value{
The input \code{fit}, augmented with \code{sir_ess}, \code{sir_ci_theta},
\code{sir_ci_omega}, \code{sir_ci_sigma}, and (when requested) \code{sir_resamples} /
\code{sir_resamples_n} / \code{sir_resamples_dim}.
}
\description{
Run a Sampling Importance Resampling (SIR) uncertainty step against a
fit that was produced earlier - useful when the original fit was
expensive and you want to add SIR without re-estimating, or when working
with a fit loaded from a \code{.fitrx} bundle.
}
\details{
\code{ferx_sir()} re-uses the fit's asymptotic covariance matrix as the SIR
proposal distribution and the per-subject empirical Bayes ETAs as
warm-starts for the inner loop. The returned fit is the input with
\code{sir_ess}, \code{sir_ci_theta}, \code{sir_ci_omega}, \code{sir_ci_sigma} (and, when
\code{sir_keep_samples = TRUE}, \code{sir_resamples} / \code{sir_resamples_n} /
\code{sir_resamples_dim}) populated.
\subsection{Integrity check}{

\code{ferx_fit()} records the model and data file paths plus SHA-256 hashes
on the returned fit (\code{fit$model_path}, \code{fit$data_path},
\code{fit$model_hash}, \code{fit$data_hash}). \code{ferx_sir()} re-reads those files
and verifies the hashes; if either file changed since the fit, the call
is a \strong{hard error}. The point of running SIR against the original fit
is to refine its uncertainty estimate, which is meaningless against a
modified model or dataset.

Edge case: if hashing failed at fit time (e.g. permission flip between
parse and hash) the corresponding \verb{fit$*_hash} is \code{NA} and the
integrity check silently passes on that side. This is rare in
practice - hashing would have to fail while the parse just
succeeded - but if you require integrity verification, check
\code{!is.na(fit$model_hash) && !is.na(fit$data_hash)} before relying on
the protection.
}
}
\examples{
\dontrun{
ex  <- ferx_example("warfarin")
fit <- ferx_fit(ex$model, ex$data, covariance = TRUE)

# Run SIR post-hoc (equivalent to sir = TRUE at fit time)
fit <- ferx_sir(fit, sir_samples = 2000, sir_resamples = 500, sir_seed = 42)

# Effective sample size - closer to sir_resamples means good coverage
fit$sir_ess

# 95\% CI for fixed-effect thetas
fit$sir_ci_theta

# 95\% CI for IIV omega diagonal
fit$sir_ci_omega

# 95\% CI for residual error sigma
fit$sir_ci_sigma

# Retain resamples for downstream uncertainty simulation
fit2 <- ferx_sir(fit, sir_samples = 2000, sir_resamples = 500,
                 sir_keep_samples = TRUE)
sims <- ferx_simulate_with_uncertainty(
  ex$model, ex$data, fit2,
  n_uncertainty_draws = 200, n_sim_per_draw = 5,
  method = "sir"
)
head(sims)
}

}
\seealso{
\code{\link[=ferx_fit]{ferx_fit()}} for the inline SIR option (\code{sir = TRUE}),
\code{\link[=ferx_simulate_with_uncertainty]{ferx_simulate_with_uncertainty()}} for downstream consumption of the
retained resamples.

Other fitting: 
\code{\link{ferx_check_init}()},
\code{\link{ferx_collect}()},
\code{\link{ferx_fit}()},
\code{\link{ferx_inits_from_nca}()},
\code{\link{ferx_stop}()},
\code{\link{print.ferx_job}()}
}
\concept{fitting}
