% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ferx_covariance.R
\name{ferx_covariance}
\alias{ferx_covariance}
\title{Run the covariance step against an existing fit}
\usage{
ferx_covariance(
  fit,
  covariance_method = "r",
  mu_referencing = TRUE,
  verbose = FALSE
)
}
\arguments{
\item{fit}{A \code{ferx_fit} object produced by \code{\link[=ferx_fit]{ferx_fit()}} or
\code{\link[=ferx_load_fit]{ferx_load_fit()}}.}

\item{covariance_method}{Covariance estimator: \code{"r"} (inverse Hessian, the
default, NONMEM \code{MATRIX=R}), \code{"s"} (inverse cross-product,
\code{MATRIX=S}), or \code{"rsr"} (the Huber-White sandwich, \code{MATRIX=RSR}).
\code{"hessian"} / \code{"cross_product"} / \code{"sandwich"} are accepted as aliases.}

\item{mu_referencing}{Use mu-referencing for the inner-loop warm restart.
Default \code{TRUE} (the engine default). Set to match the setting used for
the original fit.}

\item{verbose}{When \code{TRUE}, the engine prints progress to stderr.
Default \code{FALSE}.}
}
\value{
The input \code{fit}, with \code{cov_matrix}, \code{cor_matrix}, \code{se_theta},
\code{se_omega}, \code{se_sigma}, \code{se_kappa}, \code{covariance_status}, \code{eigenvalues},
and \code{condition_number} refreshed.
}
\description{
Run the finite-difference-Hessian covariance step against a fit that was
produced earlier - the covariance-step analogue of \code{\link[=ferx_sir]{ferx_sir()}}. Useful
when the original fit was expensive and you want to add standard errors
without re-estimating, when you want to re-run the step with a different
\code{covariance_method} (e.g. the \code{rsr} sandwich), or when working with a fit
loaded from a \code{.fitrx} bundle.
}
\details{
\code{ferx_covariance()} reconstructs the fitted parameters from the fit,
re-runs the inner loop (seeded from the per-subject empirical Bayes ETAs) to
rebuild the covariance-step inputs, and calls the same covariance
step \code{\link[=ferx_fit]{ferx_fit()}} runs inline. The result closely matches fitting with
\code{covariance = TRUE} (the same engine step; agreement is close but not
bit-exact, since the standalone re-reads the data and cold-starts the inner
EBE loop). The returned fit is the input with
\code{cov_matrix}, \code{cor_matrix}, \code{se_theta} / \code{se_omega} / \code{se_sigma} /
\code{se_kappa}, \code{covariance_status}, \code{eigenvalues}, and \code{condition_number}
refreshed.

A covariance step that runs but cannot produce a usable matrix (a
non-positive-definite or structurally-unusable FD Hessian) is \strong{not} an
error: the returned fit reports \code{covariance_status = "failed"} with a
diagnostic appended to \code{warnings}, mirroring \code{\link[=ferx_fit]{ferx_fit()}}. An error is
reserved for input problems (missing / hash-mismatched model or data).
\subsection{Integrity check}{

Like \code{\link[=ferx_sir]{ferx_sir()}}, \code{ferx_covariance()} re-reads the model and data files
recorded on the fit (\code{fit$model_path}, \code{fit$data_path}) and verifies their
SHA-256 hashes (\code{fit$model_hash}, \code{fit$data_hash}); if either file changed
since the fit, the call is a \strong{hard error}. Running the covariance step
against a modified model or dataset would be meaningless.

Edge case: if hashing failed at fit time the corresponding \verb{fit$*_hash} is
\code{NA} and the integrity check silently passes on that side. Check
\code{!is.na(fit$model_hash) && !is.na(fit$data_hash)} before relying on the
protection.
}
}
\examples{
\dontrun{
ex  <- ferx_example("warfarin")

# Fit without the covariance step, then add it post-hoc.
fit <- ferx_fit(ex$model, ex$data, covariance = FALSE)
fit <- ferx_covariance(fit)
fit$se_theta       # standard errors now populated
fit$cov_matrix     # parameter covariance matrix

# Re-run with the robust sandwich estimator instead.
fit_rsr <- ferx_covariance(fit, covariance_method = "rsr")
fit_rsr$se_theta
}

}
\seealso{
\code{\link[=ferx_fit]{ferx_fit()}} for the inline covariance step (\code{covariance = TRUE}),
\code{\link[=ferx_sir]{ferx_sir()}} for the SIR uncertainty step.

Other fitting:
\code{\link[=ferx_check_init]{ferx_check_init()}},
\code{\link[=ferx_collect]{ferx_collect()}},
\code{\link[=ferx_fit]{ferx_fit()}},
\code{\link[=ferx_inits_from_nca]{ferx_inits_from_nca()}},
\code{\link[=ferx_sir]{ferx_sir()}},
\code{\link[=ferx_stop]{ferx_stop()}},
\code{\link[=print.ferx_job]{print.ferx_job()}}
}
\concept{fitting}
