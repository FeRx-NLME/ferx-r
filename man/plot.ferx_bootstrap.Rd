% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ferx_bootstrap.R
\name{plot.ferx_bootstrap}
\alias{plot.ferx_bootstrap}
\title{Plot a bootstrap parameter distribution}
\usage{
\method{plot}{ferx_bootstrap}(x, y = NULL, parameters = NULL, breaks = "FD", ...)
}
\arguments{
\item{x}{A \code{ferx_bootstrap} object.}

\item{y}{Ignored. Present only for consistency with the \code{plot(x, y, ...)}
generic; must be \code{NULL} if supplied.}

\item{parameters}{Character vector selecting which parameters to plot.
\code{NULL} (the default) plots all of them.}

\item{breaks}{Passed to \code{\link[=hist]{hist()}}. Default \code{"FD"}.}

\item{...}{Ignored.}
}
\value{
Invisibly, the character vector of parameters plotted.
}
\description{
One histogram per parameter of the bootstrap estimates - the panel PsN
generates through xpose4's \code{boot.hist} - with the original estimate drawn as
a solid line and the confidence interval as dashed lines. When the run used
\code{dofv = TRUE}, a final panel shows the distribution of the change in
objective function value against its chi-square reference density, on
\code{chi_square_df} degrees of freedom.
}
\details{
The histograms show every replicate that produced an estimate, including any
the exclusion filters dropped from the statistics; the lines drawn over them
come from \verb{$parameters} and so reflect the included set only. The counts in
\verb{$diagnostics} say how far the two differ. (Deciding which replicate is
excluded is the engine's job - see \code{\link[=ferx_bootstrap_summarize]{ferx_bootstrap_summarize()}} to change
the criteria.)
}
\examples{
\dontrun{
bs <- ferx_bootstrap(ferx_example("warfarin")$model, samples = 200)
plot(bs)
plot(bs, parameters = c("TVCL", "TVV"))
}

}
\seealso{
Other diagnostics:
\code{\link[=check_diagnostics]{check_diagnostics()}},
\code{\link[=ferx_conddist]{ferx_conddist()}},
\code{\link[=ferx_cov_screen]{ferx_cov_screen()}},
\code{\link[=ferx_get_warnings]{ferx_get_warnings()}},
\code{\link[=ferx_runlog_iters]{ferx_runlog_iters()}},
\code{\link[=ferx_trace]{ferx_trace()}},
\code{\link[=plot.ferx_fit]{plot.ferx_fit()}},
\code{\link[=summary.ferx_fit]{summary.ferx_fit()}}
}
\concept{diagnostics}
